

interface AuthWindowProps {
    closeModal: () => void,
    login: string,
    initialState: boolean
}

const AuthWindow = ({ closeModal, login, initialState }: AuthWindowProps) => {
    return (
        <></>
    )
}

export default AuthWindow;