export type LoadingType = {
    id: symbol | number
    status: boolean
};

export type ErrorType = {
    id: symbol | number
    message: string
}

export type MessageType = {
    id: symbol | number
    message: string
}